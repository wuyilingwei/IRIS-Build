# 任务计划

- [x] 审计现行统一签名、固定旧源提交与 shell-build 顺序
- [x] 为固定旧提交增加临时 legacy-bridge 签名阶段
- [x] 在 macOS 临时信任并清理旧公开根
- [x] 补 workflow 静态映射检查并运行 YAML/actionlint 验证
- [x] 改为只接受独立的 legacy P12/password secrets，拒绝历史私钥恢复路径
